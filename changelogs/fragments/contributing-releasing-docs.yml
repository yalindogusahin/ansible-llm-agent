---
minor_changes:
  - >
    docs: add ``CONTRIBUTING.md`` covering local dev setup, pre-commit /
    pytest workflow, branching + PR conventions, the changelog-fragment
    format, examples/ domain authoring, and security reporting.
  - >
    docs: add ``RELEASING.md`` documenting the semver policy, the pre-1.0
    caveat (minor bumps may include breaking changes until 1.0), the
    1.0-onwards deprecation policy (two-minor support window before
    removal), the support matrix (ansible-core ≥2.15, Python 3.10-3.12),
    and the PR-driven release flow (galaxy.yml bump + changelog fragment
    consumption + ``v*`` tag → release.yml workflow → Galaxy publish).
