---
minor_changes:
  - >
    docs: README now points at the ``examples/`` cookbook (six domains,
    twelve playbooks) above the inline API-shape examples, with a note
    on when to reach for which.
  - >
    docs: CONTRIBUTING.md drops ``trivial`` from the recommended
    changelog-fragment section list and adds a callout — it's accepted
    by ``antsibull-changelog`` but rejected by the ``ansible-test
    sanity`` changelog rule, which is now a required check.
